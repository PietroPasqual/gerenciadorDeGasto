// Dublê gerado dos exports reais de src/services/category-rules.ts.
// Gerado por scripts/gerar-dubles.mjs — não edite à mão.
import { doFixture } from './fixture'

export async function listarRegrasAprendidas(...args: unknown[]) {
  return doFixture('category-rules.listarRegrasAprendidas', args)
}
export async function aprenderRegra(...args: unknown[]) {
  return doFixture('category-rules.aprenderRegra', args)
}
export async function esquecerRegra(...args: unknown[]) {
  return doFixture('category-rules.esquecerRegra', args)
}
