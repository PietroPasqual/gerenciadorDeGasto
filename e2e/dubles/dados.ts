// Dublê gerado dos exports reais de src/services/dados.ts.
// Gerado por scripts/gerar-dubles.mjs — não edite à mão.
import { doFixture } from './fixture'

export async function contarDados(...args: unknown[]) {
  return doFixture('dados.contarDados', args)
}
export async function apagarDados(...args: unknown[]) {
  return doFixture('dados.apagarDados', args)
}
